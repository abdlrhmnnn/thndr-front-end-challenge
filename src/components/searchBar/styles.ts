import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  searchBar: {
    backgroundColor: 'transparent',
    borderRadius: 15,
    marginBottom: 20,
    width: '90%',
    alignSelf: 'center',
    elevation: 1,
  },
});
